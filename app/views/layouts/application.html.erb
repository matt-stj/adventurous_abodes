<!DOCTYPE html>
<html>
<head>
  <title>Adventure Abodes</title>
  <link href="http://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>

  <style>
  nav ul li a {
    color: black;
  }
  </style>
</head>
  <main>

<body class="Site">
  <div class = "<%= params[:controller] %>">
    <header class="nav">
      <nav>
        <div class="nav-wrapper white">
          <a href="/" class="brand-logo black-text">Adventurous Abodes</a>
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li><%= link_to "Rentals", rentals_path %></li>
            <li><%= link_to "Become A Host", new_owner_path %>
            <li><%= join_dashboard_path %></li>
            <li><%= login_logout_path %></li>
            <li><%= trips_cart_display %></li>
          </ul>
        </div>
      </nav>
    </header>
    <div class="row">
      <div class="col s5 offset-s3 m10 offset-m3">
        <% flash.each do |type, msg| %>
        <div class="card-panel btn-large waves-effect waves-light green" >
          <h5><%= content_tag :div, sanitize(msg), class: "flash_#{type}" %></h5>
        </div>
        <% end %>
      </div>
    </div>

    <div class='container'>
      <%= yield %>
    </div>
  </main>
    <%= render "shared/footer" %>    ​

  </body>
  </html>
